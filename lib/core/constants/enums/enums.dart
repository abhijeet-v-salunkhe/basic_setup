// Enum for the Password form field validation massage
enum PasswordValidationError { empty, invalid }

// Enum for the Email form field validation message
enum EmailValidationError { empty, invalid }
